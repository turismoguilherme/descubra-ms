
import React from 'react';
import { useNavigate } from 'react-router-dom';
import FlowTripProfessional from './FlowTripProfessional';

const FlowTripSaaS = () => {
  // Redirect to the new professional design
  return <FlowTripProfessional />;
};

export default FlowTripSaaS;
