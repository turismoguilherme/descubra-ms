export function extractReadableContent(rawHtml: string): { title: string; content: string } {
  // Placeholder: usar Readability/JSDOM posteriormente
  return {
    title: 'Título detectado (placeholder)',
    content: 'Texto limpo (placeholder)'
  };
}
