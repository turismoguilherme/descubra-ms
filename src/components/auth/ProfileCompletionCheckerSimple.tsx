import React from 'react';

const ProfileCompletionCheckerSimple: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  // Versão simplificada que não faz verificações complexas
  return <>{children}</>;
};

export default ProfileCompletionCheckerSimple;

